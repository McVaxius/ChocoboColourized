name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.103'
          dotnet-quality: 'preview'

      - name: Restore
        run: dotnet restore .\ChocoboColourized.sln

      - name: Build (Release)
        run: dotnet build .\ChocoboColourized.sln -c Release --no-restore

      - name: Upload packaged plugin zip
        uses: actions/upload-artifact@v4
        with:
          name: ChocoboColourized-latest
          path: .\ChocoboColourized\bin\x64\Release\ChocoboColourized\latest.zip
          if-no-files-found: error
